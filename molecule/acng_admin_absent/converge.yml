---
- name: Converge
  hosts: all
  vars:
    acng_admin_name: test_user
    acng_admin_password: test_pass
    acng_admin_state: absent
  tasks:
    - name: "Include acng"
      include_role:
        name: "acng"
